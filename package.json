{
  "name": "lambda-bonder-transaction-validation",
  "version": "1.0.1",
  "description": "Tools to add bonder validation to AWS Lambda",
  "author": "Authereum Labs, Inc.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.12.2",
    "pnpm": ">=9"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "clean": "rm -rf node_modules dist ./tsconfig.tsbuildinfo",
    "build": "tsc -p ./tsconfig.json",
    "generate": "rm -rf lambdaBonderTransactionValidation.zip dist && esbuild ./src/index.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=dist/index.js && cd ./dist && zip -r lambdaBonderTransactionValidation.zip index.js* && mv ./lambdaBonderTransactionValidation.zip ../ && cd .."
  },
  "dependencies": {
    "ethers": "5.7.1",
    "@aws-sdk/client-kms": "3.667.0"
  },
  "devDependencies": {
    "@types/node": "22.7.5",
    "typescript": "5.6.2",
    "esbuild": "0.24.0"
  }
}
